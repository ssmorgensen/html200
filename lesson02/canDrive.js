let firstName = 'Elizabeth';
let lastName = 'Lemon';
let x = 'can drive';
let currentAge = 42;
const drivingAge = 16;
let drivingStatus = firstName + ' ' + lastName + ' ' +  x + ' ' + (currentAge - drivingAge)
for (let y = 5; y >= 0; y--)

if (currentAge - drivingAge >=0) {
  console.log (drivingStatus);
} else {
  console.log (y);
}
